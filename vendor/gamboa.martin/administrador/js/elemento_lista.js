$( document ).ready(function() {

    if(seccion === 'adm_elemento_lista' && (accion === 'alta')) {
        let select_seccion_menu_id = $("select[name=seccion_menu_id]");
        select_seccion_menu_id.change(function () {

        });

    }


});

